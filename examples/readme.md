Before (re)placing a notebook folder in the example folder run:

```
jupyter nbconvert --to notebook --ClearOutputPreprocessor.enabled=True --ClearMetadataPreprocessor.enabled=True
```

to clear all outputs and metadata. This provides a clean version for version control.